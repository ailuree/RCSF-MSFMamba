# 数据集学习与答辩说明

这份文档的目标不是只列配置参数，而是把你项目里用到的数据集讲清楚，尽量讲得通俗、可复习、可答辩。

文档会重点围绕你当前毕业设计真正用到的两个数据集：

1. `Houston2013`
2. `Houston2018`

仓库里虽然还保留了 `Berlin`、`Augsburg`、`MUUFL` 等内容，但从你当前论文结果、实验命令、Web 展示和答辩材料来看，**主角是 Houston2013 和 Houston2018**。

---

## 1. 先用一句话讲清：你的数据集到底是什么

你的任务不是普通图片分类，而是：

> 用多模态遥感数据，对每个像素所属的地物类别进行分类。

这里的“多模态”主要指：

- 高光谱图像 `HSI`
- LiDAR 辅助信息

这里的“分类”不是给整张图一个类别，而是：

- 每个像素都要判断属于哪一类
- 最终输出是一张“分类地图”

所以你处理的是：

- 遥感图像
- 像素级分类
- 多模态融合任务

---

## 2. 你的数据集在项目里长什么样

项目里的数据集主要放在：

[`data`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data)

当前你实际最关心的是：

- [`data/Houston2013`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2013)
- [`data/Houston2018`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2018)

### 2.1 Houston2013 目录

[`data/Houston2013`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2013)

里面可以看到：

- `houston_hsi.mat`
- `houston_lidar.mat`
- `houston_gt.mat`
- `houston_index.mat`
- 原始数据文件夹 `2013_DFTC`

### 2.2 Houston2018 目录

[`data/Houston2018`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2018)

里面主要是处理好的 4 个 `.mat` 文件：

- `houston_hsi.mat`
- `houston_lidar.mat`
- `houston_gt.mat`
- `houston_index.mat`

### 2.3 这 4 个 `.mat` 文件分别是什么

对于 Houston2013 和 Houston2018，这 4 个文件的职责基本一样：

- `houston_hsi.mat`：高光谱主模态
- `houston_lidar.mat`：辅助模态
- `houston_gt.mat`：真值标签图
- `houston_index.mat`：训练/测试/全部坐标索引

也就是说，你项目里并不是直接拿原始 `.tif` 训练，而是先整理成统一的 `.mat` 格式，再给训练代码使用。

---

## 3. 一定先理解的几个基础概念

## 3.1 什么是 HSI

`HSI` 是 `Hyperspectral Image`，高光谱图像。

普通照片通常只有 3 个通道：

- 红
- 绿
- 蓝

高光谱图像不是 3 个通道，而是很多很多个波段。  
你可以把它理解成：

> 每个像素不只是一个颜色，而是一整条更丰富的“光谱指纹”。

这意味着：

- 不同地物即使看起来颜色接近
- 也可能在不同波段上呈现出不同响应

所以 HSI 特别适合做地物分类。

### 通俗理解

普通 RGB 像是在“看外表”。  
高光谱更像是在“看这个东西在很多波段下的反应曲线”。

---

## 3.2 什么是 LiDAR

LiDAR 在你的项目里是辅助模态。

它通常能提供：

- 高度信息
- 结构信息
- 建筑、地面、植被之间的差异

如果只看 HSI，有些类别可能光谱接近；  
如果再加上 LiDAR，就更容易区分。

例如：

- 建筑和道路在某些情况下可能颜色接近
- 但高度差异明显

这时 LiDAR 就有帮助。

---

## 3.3 什么是 GT

`GT` 是 `Ground Truth`，也就是真值、人工标注。

它是“标准答案”。

模型训练和评估最后都要参考它。

### 你项目里的 GT 有什么特点

很重要的一点是：

> GT 不是所有像素都有标签。

因此：

- 很多像素值是 `0`
- 非零值才是真正标注好的类别

这也是为什么你前面看到的很多 GT 图是黑底。

---

## 3.4 什么是 train/test index

除了 GT 以外，你的数据集还有一个非常关键的文件：

[`houston_index.mat`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2013/houston_index.mat)

或者：

[`houston_index.mat`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2018/houston_index.mat)

这个文件不存图像本身，而是存：

- 哪些坐标属于训练集
- 哪些坐标属于测试集
- 哪些坐标属于全部可用样本

也就是说，你不是随便从整张图上采样，而是按这个索引文件来决定：

- 哪些像素拿来训练
- 哪些像素拿来测试

---

## 4. 项目里数据集配置是怎么写的

数据集配置文件在：

[`dataset_info.yaml`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/dataset_info.yaml)

这里定义了每个数据集的：

- 图像尺寸
- 通道数
- 类别数
- PCA 维数
- 窗口大小
- 网络相关超参数

对你当前最重要的两项如下。

### 4.1 Houston2013

配置里写的是：

- `hsi_channel_num: 144`
- `slar_channel_num: 1`
- `image_h: 349`
- `image_w: 1905`
- `pca_num: 30`
- `num_classes: 15`
- `window_size: 11`

### 4.2 Houston2018

配置里写的是：

- `hsi_channel_num: 50`
- `slar_channel_num: 1`
- `image_h: 1202`
- `image_w: 4768`
- `pca_num: 30`
- `num_classes: 20`
- `window_size: 11`

### 4.3 这些参数分别是什么意思

#### `hsi_channel_num`

高光谱波段数。

- Houston2013：144 个波段
- Houston2018：50 个波段

这说明 Houston2013 的光谱维度更高。

#### `slar_channel_num`

这里虽然命名沿用了原仓库写法，但对 Houston 数据来说，实际对应的是单通道 LiDAR 辅助模态。

所以这里的 `1` 表示：

- 辅助模态是单通道
- 不是 RGB 那样 3 通道

#### `image_h`, `image_w`

整张数据图的高和宽。

#### `pca_num`

训练时先把高光谱数据做 PCA 降到 30 维。

原因很简单：

- 原始高光谱维度太高
- 直接喂进去计算量大
- PCA 能保留主要信息，同时降低维度

#### `num_classes`

类别数。

- Houston2013：15 类
- Houston2018：20 类

#### `window_size`

滑窗大小，也就是每次拿一个 `11 × 11` 的局部 patch 作为输入上下文。

这意味着模型在判断中心像素类别时，不是只看一个点，而是看周围邻域。

---

## 5. 代码里是怎么读数据的

核心数据读取代码在：

[`setting/dataLoader.py`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/setting/dataLoader.py)

### 5.1 读入了哪些内容

`get_loader()` 会读取：

- `houston_hsi.mat`
- `houston_lidar.mat`
- `houston_gt.mat`
- `houston_index.mat`

然后取出：

- HSI
- 辅助模态
- GT
- 训练索引
- 测试索引

### 5.2 后续做了哪些预处理

读取后主要做了 3 件事：

1. 对 HSI 做 PCA，降到 30 维
2. 对 PCA 后 HSI 做标准化
3. 对辅助模态也做标准化

### 5.3 为什么还要切 patch

`HXDataset` 不是直接返回单个像素，而是返回：

- 一个 `11 × 11` 的 HSI patch
- 一个 `11 × 11` 的辅助模态 patch
- 该 patch 中心像素的标签

这说明你的任务虽然最后输出是像素类别，但模型判断时会看一个局部邻域。

### 5.4 标签为什么减 1

在 `__getitem__()` 里，标签做了：

`gt = torch.tensor(self.gt[h, w] - 1).long()`

原因是原始 GT 类别编号通常从 `1` 开始，  
而深度学习训练里更常用从 `0` 开始的类别编号。

所以：

- 原始标签 `1~15` / `1~20`
- 训练标签转成 `0~14` / `0~19`

---

## 6. Houston2013 详细说明

## 6.1 它在你的项目里是什么地位

Houston2013 是你当前论文和 Web 展示里的**主分析数据集**。

这一点可以从多个地方看出来：

- Web 页面主看 `Houston2013`
- 样例推理页直接用的是 `Houston2013 ROI`
- 论文中大量重点图也是 Houston2013

所以答辩时你可以把它理解为：

> 主战场、主展示对象、主分析对象

---

## 6.2 Houston2013 的原始来源

你仓库里有一个非常关键的原始文件夹：

[`data/Houston2013/2013_DFTC`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/data/Houston2013/2013_DFTC)

里面包含：

- `2013_IEEE_GRSS_DF_Contest_CASI.tif`
- `2013_IEEE_GRSS_DF_Contest_LiDAR.tif`
- `2013_IEEE_GRSS_DF_Contest_Samples_TR.txt`
- `2013_IEEE_GRSS_DF_Contest_Samples_VA.txt`

另外，你项目里还有一个准备脚本：

[`tools/prepare_houston2013.py`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/tools/prepare_houston2013.py)

从这个脚本可以明确确认：

- 你的 Houston2013 数据来自 `2013 IEEE GRSS Data Fusion Contest`
- 原始文件包括 CASI 高光谱图和 LiDAR 图
- 训练/验证标签来自公开的 `TR/VA` 文本文件

这个信息是可以从你本地仓库直接确认的，不是猜的。

### 答辩时可以怎么说

“Houston2013 数据集来自 2013 IEEE GRSS Data Fusion Contest，包含 144 波段高光谱数据和单通道 LiDAR 辅助数据，训练和测试标签由官方提供的标注文件整理而成。”

---

## 6.3 Houston2013 的尺寸和结构

实际 `.mat` 文件里读出来是：

- `houston_hsi`: `(349, 1905, 144)`
- `houston_lidar`: `(349, 1905)`
- `houston_gt`: `(349, 1905)`

这表示：

- 高度 349
- 宽度 1905
- HSI 每个像素有 144 维光谱信息
- LiDAR 是单通道

### 怎么想象这个结构

你可以把它理解成：

- 一张长条形遥感区域图
- 每个位置都有 144 维高光谱特征
- 同一位置还有 1 个 LiDAR 辅助值
- 只有部分位置有人工标签

---

## 6.4 Houston2013 的样本数量

实际索引文件里是：

- `houston_train`: `2832`
- `houston_test`: `12197`
- `houston_all`: `15029`

同时 GT 中统计到：

- 非零标注像素：`15029`
- 零值像素：`649816`

### 这说明什么

这说明 Houston2013 是一个**典型的稀疏标注数据集**。

也就是说：

- 整张图像很大
- 但真正有标签的像素只有 15029 个
- 大量像素没有标签，不参与监督训练和标准评估

### 为什么这件事很重要

因为它解释了你前面很多疑问：

- 为什么 GT 图大面积黑色
- 为什么论文图常是黑底彩色点块
- 为什么不能简单把整张图都当作真值

---

## 6.5 Houston2013 有哪些类别

类别名称定义在：

[`utility.py`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/utility.py)

具体 15 类是：

1. Healthy grass
2. Stressed grass
3. Synthetic grass
4. Trees
5. Soil
6. Water
7. Residential
8. Commercial
9. Road
10. Highway
11. Railway
12. Parking Lot 1
13. Parking Lot 2
14. Tennis Court
15. Running Track

### 通俗理解

这 15 类大致可以分成几组：

- 草地/植被类：Healthy grass、Stressed grass、Synthetic grass、Trees
- 地表/自然类：Soil、Water
- 建筑功能区类：Residential、Commercial
- 交通设施类：Road、Highway、Railway
- 特定场地类：Parking Lot 1、Parking Lot 2、Tennis Court、Running Track

### 为什么这个类别设置不简单

因为其中有些类别很容易混淆：

- `Road` 和 `Highway`
- `Residential` 和 `Commercial`
- `Parking Lot 1` 和 `Parking Lot 2`
- `Healthy grass` 和 `Stressed grass`

所以这个数据集并不是那种“颜色一眼就能分”的简单任务。

---

## 6.6 Houston2013 的一个关键特点：类别并不均衡

从 GT 统计就能看出来，不同类别样本量不是完全平均的。  
这意味着训练时会遇到：

- 有的类别很多
- 有的类别很少
- 小类更难学

这也解释了为什么你论文里不仅看 `OA`，还要看：

- `AA`
- `Macro-F1`

因为只看 `OA`，可能会被大类“带高”，但小类其实分得不好。

---

## 6.7 Houston2013 为什么适合做主分析数据集

因为它同时具备：

- 多模态：HSI + LiDAR
- 类别多：15 类
- 结构复杂：道路、停车场、建筑、植被都有
- 稀疏标注：更接近真实评估场景
- 官方数据竞赛背景：学术上常用

这使它很适合做：

- 方法对比
- 局部改进分析
- 鲁棒性分析
- 可视化展示

---

## 7. Houston2018 详细说明

## 7.1 它在你的项目里是什么角色

Houston2018 在你现在的项目里主要承担：

> 第二数据集验证、跨场景验证、辅助泛化说明

也就是说：

- Houston2013 负责主分析
- Houston2018 负责证明“不是只在一个数据集上有效”

---

## 7.2 Houston2018 的来源说明

从本地仓库可以明确确认的是：

- 你有完整的 `houston_hsi.mat / houston_lidar.mat / houston_gt.mat / houston_index.mat`
- `dataset_info.yaml` 中把它定义成独立数据集
- 训练与评估代码都原生支持 `Houston2018`
- Web 展示中使用的是 `Houston2018 small`

但是，**仓库里没有像 Houston2013 那样的原始下载/准备脚本**，所以对于 Houston2018 的更详细外部来源，当前仓库没有像 Houston2013 那样直接给出证据。

因此答辩时更稳妥的说法是：

> “Houston2018 是项目中的第二个 Houston 系列多模态遥感分类数据集，在当前工程中已经整理为高光谱、LiDAR、GT 和索引文件，用于跨数据集验证。”

这个说法是完全基于你本地工程可确认的信息。

---

## 7.3 Houston2018 的尺寸和结构

实际 `.mat` 文件读出来是：

- `houston_hsi`: `(1202, 4768, 50)`
- `houston_lidar`: `(1202, 4768)`
- `houston_gt`: `(1202, 4768)`

这说明：

- 这张图比 Houston2013 大得多
- 空间尺寸明显更大
- 但 HSI 波段数更少，只有 50 维

### 通俗理解

Houston2013 更像“光谱更丰富，图幅较小”。  
Houston2018 更像“空间更大，但光谱维数更低”。

---

## 7.4 Houston2018 的样本数量

索引文件中有：

- `houston_train`: `18750`
- `houston_test`: `2000160`
- `houston_all`: `5731136`
- `houston_test_small`: `201896`

同时 GT 统计到：

- 非零标注像素：`2018910`
- 零值像素：`3712226`

### 这组数字非常值得你理解

这说明 Houston2018 有两个很显著的特点：

1. 数据更大
2. 测试像素特别多

`houston_test` 高达两百万级，意味着如果直接完整评估：

- 计算量大
- 速度慢
- 开发调试成本高

这就是为什么你的代码里专门支持：

- `eval_split=small`
- `houston_test_small`

---

## 7.5 什么是 `Houston2018 small`

这是你答辩时非常容易被问到的点。

在：

[`setting/dataLoader.py`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/setting/dataLoader.py)

里可以看到：

- 如果数据集是 `Houston2018`
- 并且你选择 `eval_split=small`
- 那么测试集会优先使用 `houston_test_small`

也就是说：

> `Houston2018 small` 不是一个完全不同的新数据集，而是 Houston2018 的一个小测试划分。

### 它为什么存在

因为完整测试集非常大，开发和可视化时不方便。  
所以工程里保留了一个更小的评估子集。

### 它在你的项目里怎么用

- 开发期：常用 `small`
- Web 展示：用的是 `Houston2018 small`
- 论文局部展示：也更适合使用 small 结果

### 答辩时可以怎么说

“Houston2018 small 是从 Houston2018 中抽出的一个较小评估划分，用于更高效的开发、调试和展示。它不是不同来源的数据，而是同一数据集在工程中的一个小规模评估子集。”

---

## 7.6 Houston2018 的类别

在：

[`utility.py`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/utility.py)

里定义了 20 类：

1. Healthy grass
2. Stressed grass
3. Artificial turf
4. Evergreen trees
5. Deciduous trees
6. Bare earth
7. Water
8. Residential buildings
9. Non-residential buildings
10. Roads
11. Sidewalks
12. Crosswalks
13. Major thoroughfares
14. Highways
15. Railways
16. Paved parking lots
17. Unpaved parking lots
18. Cars
19. Trains
20. Stadium seats

### 这 20 类相比 Houston2013 有什么变化

Houston2018 更细。

例如：

- 树木细分为常绿和落叶
- 建筑细分为住宅与非住宅
- 交通类更细，出现 sidewalk、crosswalk、major thoroughfares
- 停车场细分为铺装与未铺装
- 还加入了 `Cars`、`Trains`、`Stadium seats`

### 这说明什么

这说明 Houston2018 的类别定义更精细、场景更复杂。

从答辩角度，你可以说：

> 它比 Houston2013 在类别粒度上更细，因此对模型区分能力提出了更高要求。

---

## 8. 两个数据集的对比理解

下面这张表是你最适合记住的。

| 项目         | Houston2013           | Houston2018            |
| ------------ | --------------------- | ---------------------- |
| 角色         | 主分析数据集          | 辅助验证数据集         |
| HSI 波段数   | 144                   | 50                     |
| 辅助模态     | LiDAR 单通道          | LiDAR 单通道           |
| 图像尺寸     | 349 × 1905            | 1202 × 4768            |
| 类别数       | 15                    | 20                     |
| 训练样本     | 2832                  | 18750                  |
| 测试样本     | 12197                 | 2000160                |
| small 测试集 | 无                    | 201896                 |
| 工程展示重点 | 全局图、ROI、样例推理 | small 结果、跨场景验证 |

### 你可以怎么概括它们的差异

一句话版：

> Houston2013 更适合做主分析和细节展示，Houston2018 更适合做更大规模、更细粒度的跨数据集验证。

---

## 9. 你的数据集到底“怎么来的”

这个问题在答辩里很常见，但要分两层回答。

## 9.1 原始层面

对于 Houston2013，本地仓库可以明确确认：

- 原始高光谱图来自 CASI TIFF
- 原始辅助模态来自 LiDAR TIFF
- 原始训练/验证标注来自官方文本文件

这一点来自：

[`tools/prepare_houston2013.py`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/tools/prepare_houston2013.py)

## 9.2 工程层面

在你的项目里，数据不是直接拿原始 `.tif` 来训练，而是做成统一的 `.mat`：

- `houston_hsi.mat`
- `houston_lidar.mat`
- `houston_gt.mat`
- `houston_index.mat`

也就是说，数据的来路可以这样讲：

1. 原始遥感数据和标注先下载/整理
2. 再转换成工程统一使用的 `.mat`
3. 训练代码从 `.mat` 中读取模态、标签和索引

### 这样讲的好处

这样答辩时既不会太空泛，也不会陷入细枝末节。

---

## 10. 项目里的数据预处理流程

你可以把数据预处理理解成一条很清楚的链路：

1. 读取 HSI、LiDAR、GT、index
2. 对 HSI 做 PCA，降到 30 维
3. 对 PCA 后 HSI 做标准化
4. 对 LiDAR 做标准化
5. 根据索引提取中心像素坐标
6. 以中心像素为中心裁 `11 × 11` patch
7. 把 patch 和中心像素标签喂给模型

### 为什么不直接整图训练

因为：

- 整图太大
- 标签是按像素给的
- 用 patch 做局部上下文分类更方便、更常见

### 为什么既保留原 HSI 又保留 PCA 后 HSI

从当前代码看：

- 数据集中会同时组织原 HSI patch 和 PCA 后 HSI patch
- 实际模型前向更偏向使用 PCA 后输入

这么做的目的通常是：

- 降维降低成本
- 保留可扩展性

---

## 11. 数据集里的“0”到底是什么意思

这个问题你一定要彻底理解。

在 GT 图中：

- `0` 不表示一个正式类别
- `0` 表示未标注背景

在可视化里，`0` 通常会被映射成黑色。

所以：

- 黑色不是“黑土地”
- 也不是“未知类别的一种真实语义类”
- 它只是“这里没有参与标注/评估”

这点在结果图解释里非常关键。

---

## 12. 数据集样例应该怎么理解

你已经有一些非常适合拿来讲的样例图：

- ![`roi_hsi.png`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/web-showcase-app/public/showcase/demo/Houston2013/roi_hsi.png)
- ![`roi_aux.png`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/web-showcase-app/public/showcase/demo/Houston2013/roi_aux.png)
- ![`roi_gt.png`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/web-showcase-app/public/showcase/demo/Houston2013/roi_gt.png)
- ![`roi_prediction.png`](/G:/temp/A-BiYe/BiShe3/5-MyProject/A-MyCode/web-showcase-app/public/showcase/demo/Houston2013/roi_prediction.png)

这四张图刚好能把一个数据样例讲完整。

### `roi_hsi.png`

说明原始高光谱场景的伪 RGB 外观。

### `roi_aux.png`

说明同一区域的辅助模态结构。

### `roi_gt.png`

说明这一块区域有哪些位置有真值标签。

### `roi_prediction.png`

说明模型最后把整块区域分成了什么。

### 这四张图一起讲时的逻辑

可以这样说：

“同一个 ROI 区域，我先展示主模态 HSI 伪 RGB，再展示辅助模态 LiDAR，然后给出局部 GT 作为标准答案，最后展示模型输出的密集预测图。这样就能把输入、真值和输出放在同一个局部场景里理解。”

---

## 13. 为什么你的数据集适合做多模态分类研究

这是答辩里很常见的价值问题。

你可以从 4 个角度回答。

### 13.1 模态互补性强

HSI 提供丰富光谱信息，LiDAR 提供结构/高度信息。  
两者不是重复，而是互补。

### 13.2 类别复杂

无论 Houston2013 还是 Houston2018，都不是几类简单地物，而是包含：

- 植被
- 土地
- 建筑
- 道路
- 交通设施
- 运动场地

### 13.3 存在容易混淆的细粒度类别

例如：

- Road vs Highway
- Residential vs Commercial
- 不同种类 grass
- 不同种类 parking lot

### 13.4 评估更接近真实研究问题

因为标注不是满图全覆盖，模型需要在有限标注条件下完成分类任务，这更贴近很多遥感研究的真实情况。

---

## 14. 你在答辩时最容易被问的几个问题

## 14.1 你的数据集有几种输入

答：

两种主输入模态：

- 高光谱 HSI
- LiDAR 辅助模态

---

## 14.2 你的标签是怎么组织的

答：

标签是像素级 GT 图。  
非零值表示具体类别编号，零值表示未标注背景。  
训练和测试样本则由 `houston_index.mat` 中的坐标索引控制。

---

## 14.3 为什么要做 PCA

答：

因为高光谱原始维度太高，直接训练成本较大。  
PCA 可以在保留主要信息的前提下降维到 30 维，提高训练效率。

---

## 14.4 为什么是 11×11 patch

答：

因为分类中心像素时，周围邻域信息也很重要。  
`11 × 11` patch 让模型既看中心点，也看局部空间结构。

---

## 14.5 为什么 Houston2018 要有 small

答：

因为完整测试集太大，开发和展示成本高。  
所以工程里保留了 `houston_test_small` 作为小规模评估划分。

---

## 14.6 为什么 GT 图很多地方是黑的

答：

因为这些位置没有标签，黑色表示未标注背景，不是一个正式类别。

---

## 15. 适合你直接背的“数据集介绍口径”

如果老师让你介绍数据集，你可以直接按下面这段说：

“本项目主要使用两个 Houston 系列多模态遥感数据集，即 Houston2013 和 Houston2018。每个数据集都包含高光谱主模态、LiDAR 辅助模态、像素级真值标签以及训练测试坐标索引。Houston2013 是我的主分析数据集，尺寸为 349×1905，高光谱维度为 144，包含 15 个地物类别；Houston2018 主要用于跨数据集验证，尺寸为 1202×4768，高光谱维度为 50，包含 20 个类别。由于高光谱维度较高，工程中先通过 PCA 将其降到 30 维，再结合 11×11 的局部 patch 进行训练。GT 中 0 表示未标注背景，非零值表示真实类别标签，因此这些数据集属于典型的多模态、像素级、稀疏标注遥感分类任务。”  

---

## 16. 你复习时的建议顺序

如果你准备答辩，建议按这个顺序复习：

1. 先记住 Houston2013 和 Houston2018 各自的角色
2. 再记住 4 个核心文件：`hsi / lidar / gt / index`
3. 再记住两个关键数字：
   `Houston2013: 144 波段, 15 类`
   `Houston2018: 50 波段, 20 类`
4. 再理解 `0 是未标注，不是正式类别`
5. 再理解 `PCA 30 维 + 11×11 patch`
6. 最后再回到你的结果图和 Web 演示，把输入、标签和预测连起来看

如果这 6 步你能讲清，数据集部分基本就够支撑答辩了。

