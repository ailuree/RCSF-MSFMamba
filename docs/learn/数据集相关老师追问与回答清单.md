# 数据集相关老师追问与回答清单

这份文档的目标很明确：

- 不再系统介绍数据集
- 专门准备**老师可能追问的问题**
- 每个问题都给出一个适合答辩现场直接说出口的回答

你可以把它理解成“答辩现场应答稿”。

建议使用方式：

1. 先看问题，自己尝试回答
2. 再看文档里的参考答案
3. 最后把答案改成你自己更顺口的说法

---

## 1. 你的数据集到底是什么类型的数据集

### 老师可能会问

“你这个数据集到底是什么，不是普通图像分类吧？”

### 回答示例

“不是普通图像分类。我的数据集是多模态遥感像素级分类数据集。输入包括高光谱图像和 LiDAR 辅助模态，输出不是整张图一个类别，而是对每个像素所属的地物类别进行判断，最后生成整张分类图。”

### 回答重点

- 说清“多模态”
- 说清“像素级分类”
- 说清“不是整图分类”

---

## 2. 你的数据集包括哪些输入

### 老师可能会问

“你模型到底输入了什么？”

### 回答示例

“主要输入有两种模态。第一种是高光谱图像 HSI，它给每个像素提供丰富的光谱信息；第二种是 LiDAR 辅助模态，它提供额外的结构或高度信息。模型会把这两种模态融合起来做像素级分类。”

### 如果老师继续问“是不是就两张图”

“从展示角度看是两张模态图，但从数据结构看，不是普通图片。HSI 每个像素对应很多波段，LiDAR 是单通道辅助信息，所以本质上是多维遥感数据，不是普通 RGB 图片。”

---

## 3. 什么是高光谱，为什么不用普通 RGB

### 老师可能会问

“为什么要用高光谱，不用普通 RGB 行不行？”

### 回答示例

“普通 RGB 只有 3 个通道，而高光谱有更多波段信息，可以更细致地反映不同材料在不同波段下的响应差异。对于一些外观看起来很像的地物，比如不同类型的草地、道路和停车区域，只看 RGB 容易混淆，而高光谱更容易把它们区分开。”

### 回答重点

- RGB 是 3 通道
- HSI 有更多波段
- 类别相似时 HSI 更有优势

---

## 4. 什么是 LiDAR，它在这里有什么作用

### 老师可能会问

“LiDAR 在你这个任务里具体起什么作用？”

### 回答示例

“LiDAR 在这里是辅助模态，主要提供与光谱不同的补充信息，比如高度或空间结构信息。这样模型不只看光谱特征，还能利用结构差异来区分类别，尤其在建筑、道路、植被等容易混淆的区域会更有帮助。”

### 如果老师追问“有没有可能不用 LiDAR”

“可以做单模态实验，但区分能力通常会下降。因为有些类别在光谱上接近，只靠 HSI 不一定足够，而 LiDAR 能提供互补信息，所以多模态融合更有价值。”

---

## 5. 你的数据集是哪来的

### 老师可能会问

“你的数据集是自己采的吗，还是公开数据集？”

### 回答示例

“我当前实验主要用的是两个 Houston 系列数据集。Houston2013 在本地工程里可以明确对应到 2013 IEEE GRSS Data Fusion Contest 的原始数据，包括 CASI 高光谱图、LiDAR 图和官方训练验证标注。Houston2018 在工程里也已经整理成统一格式，用于第二数据集验证。”

### 更稳妥的讲法

如果你不想把 Houston2018 来源说得过满，可以这样说：

“这两个数据集在我的工程里都已经整理成统一的高光谱、LiDAR、GT 和索引文件格式，其中 Houston2013 的竞赛来源在本地脚本和原始文件中可以直接确认，Houston2018 主要作为第二数据集用于跨场景验证。”

---

## 6. 你的数据集为什么选 Houston2013 和 Houston2018

### 老师可能会问

“为什么选这两个数据集，不选别的？”

### 回答示例

“主要因为它们都属于典型的多模态遥感分类数据，既有高光谱主模态，也有 LiDAR 辅助模态，适合验证多模态融合方法。Houston2013 更适合做主分析，因为类别清楚、结果图和 ROI 展示都比较成熟；Houston2018 则更适合做第二数据集验证，用来说明方法不是只对一个场景有效。”

### 回答重点

- 模态合适
- 任务匹配
- 一个主分析，一个辅助验证

---

## 7. 这两个数据集各自扮演什么角色

### 老师可能会问

“你论文里为什么对 Houston2013 讲得更多？”

### 回答示例

“因为在我当前工作中，Houston2013 是主分析数据集，用来展示整体结果、局部 ROI 改进和样例推理；Houston2018 主要承担第二数据集验证的角色，用来补充说明方法在另一个场景下仍然有效，所以展示深度会相对少一些。”

---

## 8. 你的数据集有多少类

### 老师可能会问

“你这个数据集一共有多少类？”

### 回答示例

“Houston2013 有 15 个类别，Houston2018 有 20 个类别。它们都包含植被、土壤、水体、建筑、道路、停车场和运动场地等类型，其中 Houston2018 的类别划分更细，因此分类难度也更高。”

### 如果老师追问“哪些类比较难”

“比较容易混淆的通常是功能接近或外观接近的类别，比如 Road 和 Highway，Residential 和 Commercial，不同 grass 类别，以及不同 parking lot 类别。”

---

## 9. 这些类别为什么难分

### 老师可能会问

“这些类别为什么会难分？”

### 回答示例

“难点主要有两类。第一类是光谱或外观接近，比如不同类型草地、不同停车区域；第二类是空间结构复杂，比如道路、铁路、建筑边界等细长或碎片化区域。也正因为这些难点，多模态融合和鲁棒训练才有意义。”

---

## 10. 你的标签是什么形式

### 老师可能会问

“你的标签是整图标注，还是怎么标的？”

### 回答示例

“标签是像素级标签图，也就是 GT。每个有标注的像素都有一个类别编号，对应具体地物类别。没有标注的位置是 0，不参与标准监督训练和评估。”

### 回答重点

- 像素级标签
- 非零是有效类别
- 0 是未标注

---

## 11. 为什么 GT 图很多地方是黑色

### 老师可能会问

“为什么你的 GT 图看起来大部分是黑的？”

### 回答示例

“因为这个数据集不是满图密集标注，而是稀疏标注。GT 中大量像素值是 0，表示未标注背景，在可视化时通常映射成黑色。真正有类别标签的位置才会显示成不同颜色。”

### 这题非常重要

这是高频题，你最好能脱口而出。

---

## 12. 0 到底是不是一种类别

### 老师可能会问

“那 0 算不算一个类别？”

### 回答示例

“在我的任务定义里，0 不算正式语义类别，它表示未标注背景。真正参与分类的类别是从 1 开始编号的，训练时再映射成从 0 开始的索引。”

---

## 13. 你的训练集和测试集是怎么划分的

### 老师可能会问

“训练集和测试集怎么分的，是你自己随机分的吗？”

### 回答示例

“不是训练时临时随机分的，而是由数据集对应的索引文件控制。项目里有 `houston_index.mat`，里面明确给出了训练坐标、测试坐标和全部坐标，代码按这些索引去提取样本。”

### 如果老师追问“那你有没有自己改划分”

“主实验遵循已有索引划分，保证不同方法之间是在同一数据划分下公平比较。”

---

## 14. Houston2013 的训练测试规模大概是多少

### 老师可能会问

“你主数据集样本量大概多少？”

### 回答示例

“Houston2013 在当前工程里有 2832 个训练样本坐标，12197 个测试样本坐标，总标注样本是 15029 个。这也说明它是典型的稀疏标注数据集，不是整张图每个像素都参与监督。”

---

## 15. Houston2018 为什么还有 small

### 老师可能会问

“你为什么写 Houston2018 small，它和 Houston2018 是什么关系？”

### 回答示例

“Houston2018 small 不是完全不同的数据集，而是 Houston2018 在工程中使用的一个较小评估划分。因为完整 Houston2018 测试规模非常大，所以开发、调试和展示时常用 `test_small`，这样效率更高，也更适合可视化展示。”

### 如果老师追问“那会不会影响结论”

“它更适合开发和展示场景，正式结论要结合完整实验设定来解释。但作为第二数据集验证，它可以有效说明方法在另一场景下仍然保持较好的表现趋势。”

---

## 16. 为什么 Houston2018 比 Houston2013 更难

### 老师可能会问

“这两个数据集哪个更难，为什么？”

### 回答示例

“从我的理解看，Houston2018 在类别粒度和空间规模上都更复杂。它有 20 个类别，比 Houston2013 的 15 类更细；同时它图幅更大、测试样本更多，所以在工程处理和细粒度区分上都更有挑战。”

---

## 17. 为什么要做 PCA

### 老师可能会问

“高光谱为什么要先 PCA，不直接用原始波段吗？”

### 回答示例

“因为高光谱维度很高，尤其 Houston2013 有 144 个波段，直接使用计算成本更大。工程里先把 HSI 用 PCA 降到 30 维，是为了在保留主要光谱信息的同时降低训练和推理成本。”

### 如果老师追问“会不会丢信息”

“会有一定压缩，但 PCA 的目的就是尽量保留主要信息、去掉冗余维度。在当前任务里，这是一种比较常见且实用的预处理方式。”

---

## 18. 为什么 PCA 降到 30 维

### 老师可能会问

“为什么是 30，不是 20 或 50？”

### 回答示例

“当前工程配置中统一使用 30 维，主要是作为性能和计算成本之间的折中。维度太低可能损失较多信息，维度太高又会增加训练开销，所以这里沿用了项目中的稳定设置。”

### 更稳妥的说法

如果老师继续深挖，你可以说：

“这个值在当前工程里是经验配置，我的重点工作主要放在多模态融合与鲁棒性改进上，而不是重新专门调 PCA 维数。”

---

## 19. 为什么你的模型不是直接整图输入

### 老师可能会问

“为什么不整张图一起送进去？”

### 回答示例

“因为当前任务是像素级分类，但图像尺寸较大，而且监督标签也是以像素为中心组织的。工程中采用的是 patch 方式，以中心像素为目标，提取周围 `11×11` 邻域作为输入，这样既能利用局部空间上下文，也更符合当前模型结构和计算规模。”

---

## 20. 为什么是 11×11 patch

### 老师可能会问

“为什么 patch 大小是 11×11？”

### 回答示例

“11×11 的 patch 能让模型在判断中心像素类别时，同时看到附近邻域的空间结构信息。这个大小在当前工程里是一个兼顾局部上下文和计算成本的设置，不会只看单像素，也不会把邻域开得过大。”

---

## 21. 你的数据是不是做了归一化或标准化

### 老师可能会问

“输入数据有没有做归一化处理？”

### 回答示例

“有。代码里对 PCA 后的高光谱特征做了标准化，对辅助模态也做了标准化。这样可以减少不同维度和不同模态之间的数值尺度差异，便于模型训练。”

---

## 22. 你的数据增强做了什么

### 老师可能会问

“数据增强做了吗？”

### 回答示例

“在训练数据集的 patch 级别，工程里做了简单的翻转增强。也就是对 HSI patch、辅助模态 patch 和 PCA 后 patch 一起同步翻转，以增加局部样本的变化。”

### 如果老师追问“为什么只做这么简单”

“因为我当前工作的重点不在复杂数据增强，而在多模态结构改进和鲁棒训练策略。简单几何增强已经能提供一定帮助，同时不会引入额外不确定性。”

---

## 23. 你的数据集是不是类别不平衡

### 老师可能会问

“类别分布平衡吗？”

### 回答示例

“不是完全平衡的，尤其在稀疏标注场景下，不同类别的样本数会有差异。这也是为什么我在结果分析里不只看 OA，还会看 AA 和 Macro-F1，因为它们能更好反映类别均衡情况下的分类质量。”

---

## 24. 为什么不只看 OA

### 老师可能会问

“你为什么要看这么多指标，OA 不够吗？”

### 回答示例

“OA 反映整体准确率，但在类别不均衡时可能被大类主导，所以不够全面。AA 更关注每一类平均表现，Macro-F1 也能反映类别均衡性，Kappa 则能从一致性角度补充评估。因此多指标联合看会更可靠。”

---

## 25. 你的数据集适合验证什么能力

### 老师可能会问

“你这个数据集最适合验证你方法的哪种能力？”

### 回答示例

“我觉得主要是三方面。第一，多模态融合能力，因为同时有 HSI 和 LiDAR；第二，复杂类别区分能力，因为有不少细粒度和易混类别；第三，鲁棒性和泛化能力，因为我不仅在 Houston2013 上做主分析，也在 Houston2018 上做第二数据集验证。”

---

## 26. 你的样例推理图和论文里的黑底图为什么不一样

### 老师可能会问

“你网页上的推理图和彩色论文图看起来不一样，为什么？”

### 回答示例

“因为它们服务的目的不同。论文里的很多图主要是分析图，强调 GT、方法对比和改正像素分布，所以常常只突出有标签区域，黑底更多；而网页里的样例推理图是密集预测结果图，强调模型最终把整个 ROI 分成了什么，更适合现场演示。”

---

## 27. 你的数据集有没有噪声或困难场景

### 老师可能会问

“你这个数据有没有比较困难的情况？”

### 回答示例

“有。困难主要来自几个方面，比如类别间光谱相近、边界复杂、细长结构多、类别不均衡以及模态退化风险。也正因为如此，我在后续实验里专门设计了 Drop HSI、Drop Aux、Noise 和 Combined 等退化评估模式，来测试模型鲁棒性。”

---

## 28. 为什么你的方法需要第二个数据集

### 老师可能会问

“一个数据集做得很好不就够了吗，为什么还要第二个？”

### 回答示例

“一个数据集做得好只能说明方法在那个场景上有效，但不能充分说明泛化性。加入第二个数据集，尤其是类别更细、图幅更大的 Houston2018，可以更有说服力地说明方法不是只对单一场景有效。”

---

## 29. 你的数据集有没有局限性

### 老师可能会问

“你的数据集本身有没有局限？”

### 回答示例

“有。首先它仍然是特定区域的遥感场景，不代表所有地理环境；其次很多标签是稀疏标注，因此可监督样本有限；另外不同数据集之间的类别定义也不完全一致，所以跨数据集比较主要反映趋势，而不是绝对一一对应。”

### 这题回答原则

- 承认局限
- 不要否定自己工作
- 强调结论边界

---

## 30. 如果换一个数据集，你的方法还有效吗

### 老师可能会问

“如果换数据集，你的方法是不是就不行了？”

### 回答示例

“从当前实验看，在 Houston2013 和 Houston2018 两个数据集上都能看到方法带来的收益，说明它并不是只对单一数据集有效。当然，是否能推广到更多遥感场景，还需要进一步在更多数据集上验证，但至少目前已经有跨数据集结果支撑。”

---

## 31. 你为什么没有自己采数据

### 老师可能会问

“为什么不用自己采集的数据？”

### 回答示例

“当前工作的重点是方法设计、训练策略和结果分析，所以我优先使用公开标准数据集。这样做的好处是数据来源规范、便于和已有工作比较，也更适合验证模型改进本身的有效性。”

---

## 32. 你的数据集和你方法之间最匹配的点是什么

### 老师可能会问

“为什么说你的方法适合这个数据集？”

### 回答示例

“因为这个任务天然是多模态像素级分类，而我的方法核心就在于多模态融合、状态交互和鲁棒训练。数据集中既有高光谱和 LiDAR 的互补关系，也有复杂类别和退化风险，所以它能够比较完整地体现我的方法设计价值。”

---

## 33. 你最怕老师问但其实很好答的一题

### 老师可能会问

“一句话说清你的数据集特点。”

### 回答示例

“我的数据集是典型的多模态遥感像素级分类数据，输入是高光谱和 LiDAR，标签是稀疏像素级真值，既有复杂类别又有跨数据集验证需求，因此很适合验证多模态融合和鲁棒分类方法。”

---

## 34. 最适合你背的一段“老师追问统一口径”

如果你现场紧张，可以优先背下面这段：

“本项目使用的主要是 Houston2013 和 Houston2018 两个多模态遥感分类数据集。输入包括高光谱主模态和 LiDAR 辅助模态，任务是对每个像素进行地物类别识别，因此属于像素级分类问题。GT 中 0 表示未标注背景，非零值表示具体类别。工程里通过索引文件划分训练集和测试集，并先对高光谱做 PCA 降维，再提取 11×11 局部 patch 进行训练。Houston2013 主要用于主分析和可视化展示，Houston2018 主要用于第二数据集验证，说明方法不是只对单一场景有效。”  

---

## 35. 你最后要达到的答辩状态

关于数据集这一部分，你不需要追求“像论文作者一样讲得特别学术”。  
你只要做到下面 5 点，基本就够用了：

1. 能说清数据集是“多模态 + 像素级分类”
2. 能说清输入是 “HSI + LiDAR”
3. 能说清 GT 里的 `0` 是未标注
4. 能说清 Houston2013 和 Houston2018 各自扮演什么角色
5. 能说清为什么要 PCA、为什么要 patch、为什么要第二数据集

如果这 5 点你能稳定回答，老师在数据集部分一般就很难把你问住。

